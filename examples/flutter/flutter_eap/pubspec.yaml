name: flutter_eap
description: "Flutter FFI plugin for the Skyle eye tracker — Dart bindings and native bridge over the prebuilt skylelib C library (gaze, positioning, video, calibration)."
homepage: https://github.com/eyev-de/skylelib
# Version tracks the skylelib SDK version (single SemVer for the whole SDK,
# bumped by tag.sh). The native builds download the GitHub release asset
# matching this version when no local dist/ is available.
version: 0.1.6

environment:
  sdk: ^3.7.0
  flutter: ">=3.27.0"

dependencies:
  crypto: ^3.0.3
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2
  collection: ^1.19.1
  ffi: ^2.1.0
  riverpod: ^3.2.1
  riverpod_annotation: ^4.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  ffigen: ^20.1.1
  build_runner: ^2.4.13
  riverpod_generator: ^4.0.3

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' specifies the class (in Java, Kotlin, Swift, Objective-C, etc.)
  # which should be registered in the plugin registry. This is required for
  # using method channels.
  # The Android 'package' specifies package in which the registered class is.
  # This is required for using method channels on Android.
  # The 'ffiPlugin' specifies that native code should be built and bundled.
  # This is required for using `dart:ffi`.
  # All these are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    platforms:
      android:
        package: de.eyev.flutter_eap
        pluginClass: FlutterEapPlugin
      ios:
        pluginClass: FlutterEapPlugin
      macos:
        pluginClass: FlutterEapPlugin
      windows:
        pluginClass: FlutterEapPluginCApi

  # To add assets to your plugin package, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  #
  # For details regarding assets in packages, see
  # https://flutter.dev/assets-and-images/#from-packages
  #
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # To add custom fonts to your plugin package, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts in packages, see
  # https://flutter.dev/custom-fonts/#from-packages
